% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_path_info.R
\name{get_path_info}
\alias{get_path_info}
\title{Extract Path Segments or Filenames from File Paths}
\usage{
get_path_info(paths, n = NULL, rm_extension = TRUE, rm_path = TRUE)
}
\arguments{
\item{paths}{A \code{character} vector of file system paths.
Supports mixed separators (\code{/} and \verb{\\\\}) and Windows drive letters (e.g. \verb{C:}).}

\item{n}{A \code{numeric} segment index. Defaults to \code{NULL} (enters filename mode).
\itemize{
\item Positive integer: forward index from the path start; \code{1} = first segment.
\item Negative integer: reverse index from the path end; \code{-1} = last segment
(i.e. the filename segment).
\item Length-2 vector: extract a contiguous range, e.g. \code{c(2, 4)} or \code{c(-3, -1)}.
\item \code{0} is not allowed.
}}

\item{rm_extension}{A \code{logical(1)} flag controlling extension removal.
Defaults to \code{TRUE}.
\itemize{
\item In Mode B (\code{n = NULL}): always applied.
\item In Mode A: \strong{only applied when \code{n == -1}} (explicitly targeting the filename
segment). Has no effect for intermediate directory segments (e.g. \code{n = 2}).
}}

\item{rm_path}{A \code{logical(1)} flag controlling whether the directory prefix is
stripped, keeping only the filename. Defaults to \code{TRUE}.
Only applies in Mode B (\code{n = NULL}); ignored when \code{n} is specified.}
}
\value{
A \code{character} vector of the same length as \code{paths}:
\itemize{
\item Returns the extracted segment string when the segment exists.
\item Returns \code{NA_character_} when the segment index exceeds the path depth,
the input element is \code{NA}, or the path reduces to empty after normalisation
(e.g. \code{"C:/"}, \code{"/"}).
}
}
\description{
\code{get_path_info} is a merged, upgraded replacement for \code{get_path_segment} and
\code{get_filename}. It operates in two modes:
\itemize{
\item \strong{Mode A (when \code{n} is specified)}: Extract a specific path segment by
position, supporting forward indexing, reverse indexing, and range extraction.
\item \strong{Mode B (when \code{n = NULL})}: Extract the filename, with optional removal
of the file extension and/or the directory prefix.
}
}
\details{
\strong{Path normalisation (internal, fully vectorised):}
\enumerate{
\item All backslashes and consecutive slashes are collapsed to a single \code{/}.
\item Windows drive letter prefixes (\verb{C:}, \verb{D:}, etc.) are stripped.
\item Leading and trailing \code{/} characters are removed.
\item Paths that are empty after the above steps (e.g. original inputs \code{"C:/"},
\code{"/"}, \code{""}) are coerced to \code{NA_character_}.
}

\strong{Extension-stripping behaviour (internal \code{.strip_ext} helper):}\tabular{lll}{
   Input \tab Output \tab Notes \cr
   \code{"report.txt"} \tab \code{"report"} \tab Standard file — last extension removed \cr
   \code{"data.tar.gz"} \tab \code{"data.tar"} \tab Compound extension — only last level removed \cr
   \code{".bashrc"} \tab \code{".bashrc"} \tab Pure dot-file (no second dot) — unchanged \cr
   \code{".report.xlsx"} \tab \code{".report"} \tab Dot-file with extension — extension removed \cr
   \code{"no_ext"} \tab \code{"no_ext"} \tab No extension — returned as-is \cr
   \code{"file."} \tab \code{"file."} \tab Trailing isolated dot — returned as-is \cr
}


\strong{NA safety:}
\code{strsplit(NA_character_, ...)} returns \code{list(NA)} with \code{length} 1, not
\code{character(0)}. Consequently, every \code{vapply} callback guards against NA paths
with an explicit \code{anyNA(x)} check rather than \code{length(x) == 0}.
}
\examples{
paths <- c("C:/Users/foo/Documents/report.xlsx",
           "/home/user/.bashrc",
           "relative/path/to/data.csv",
           ".hidden.tar.gz",
           NA_character_)

# Mode B: filename only, extension stripped (default)
get_path_info(paths)

# Mode B: filename only, extension preserved
get_path_info(paths, rm_extension = FALSE)

# Mode B: full normalised path, extension stripped
get_path_info(paths, rm_path = FALSE)

# Mode A: extract the 2nd path segment
get_path_info(paths, n = 2)

# Mode A: extract the last segment with extension stripped (n = -1 linkage)
get_path_info(paths, n = -1, rm_extension = TRUE)

# Mode A: range extraction
get_path_info(paths, n = c(2, 3))
}
\seealso{
\code{\link[base:basename]{base::basename()}}, \code{\link[tools:fileutils]{tools::file_path_sans_ext()}}
}
